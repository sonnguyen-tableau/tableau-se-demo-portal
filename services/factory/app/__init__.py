"""Tableau AI Portal — Demo Factory.

The package is organized stage-by-stage following the pipeline:
scrape → profile → schema → generate → hyper → publish → workbook → pulse → brand.
"""

__version__ = "0.1.0"
